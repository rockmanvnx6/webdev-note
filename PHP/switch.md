# Switch

```php
$user_type = 'customer';
switch ($user_type) {
    case 'student':
        echo "welcome!";
        break;
    case 'press':
    case 'customer':
        cho "this is both case press and customer";
        break;
}
```

